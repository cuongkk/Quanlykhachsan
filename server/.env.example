DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
# Khi backend & DB đều ở Render (Postgres), dùng Internal URL thì nhanh & an toàn hơn.
# PGSSL=disable    # chỉ khi chạy local không SSL
# Giới hạn CORS (khuyên dùng khi lên Prod)
# ALLOW_ORIGIN=https://your-static-site.onrender.com
